# Generated by gloat - https://github.com/gloat/gloat
# This Makefile uses Makes to auto-install Go

M := .cache/makes
$(shell [ -d $M ] || git clone -q https://github.com/makeplus/makes $M)

include $M/init.mk
include $M/go.mk
include $M/clean.mk
include $M/shell.mk

BINARY := gist
GO-SUM := go.sum

MAKES-CLEAN := \
  $(BINARY) \
  $(GO-SUM) \


default:: build

build: $(BINARY)

$(BINARY): $(GO-SUM)
	go build -o $(BINARY) ./cmd/gist

$(GO-SUM): $(GO)
	go mod tidy
